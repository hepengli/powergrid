# Core dependencies
numpy>=1.21.0,<2.0  # Ray RLlib 2.9.0 requires numpy <2.0
pandas>=1.3.0
gymnasium>=1.0.0
pandapower>=2.14.0,<3.0  # Newer versions require numpy 2.0

# Multi-agent RL dependencies
pettingzoo>=1.24.0
ray[rllib]==2.9.0
torch>=2.0.0

# Optimization (optional but recommended)
pyscipopt>=4.0.0

# XML parsing (required by pandapower)
lxml>=4.9.0

# Visualization (optional)
matplotlib>=3.5.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.1.0
black>=22.0.0

# Logging (optional)
wandb>=0.15.0
tensorboard>=2.13.0
